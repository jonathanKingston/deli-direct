{
  "name": "delidrop",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "init": "rm -rf dist && git clone --single-branch --branch gh-pages git@github.com:jonathanKingston/deli-direct.git dist && cd dist",
    "serve": "npm run build && http-server dist/",
    "build": "cd dist && git rm --ignore-unmatch -rf * && cd ../ && node -r esm ./scripts/generate.js",
    "publish": "git add . && git commit -m \"Latest build\" && git push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "uuid": "^7.0.2"
  },
  "devDependencies": {
    "esm": "^3.2.25",
    "http-server": "^0.12.1"
  }
}
